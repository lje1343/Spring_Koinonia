diff a/src/main/resources/templates/product/sell_list.html b/src/main/resources/templates/product/sell_list.html	(rejected hunks)
@@ -154,9 +154,19 @@
 <script th:inline="javascript">
     let pno = [[${pno}]];
     let paging = [[${criteria.listLink}]];
+    let $cateForm = $(cateForm);
 
 
+    $("a.changePage").click(function(e){
+        e.preventDefault();
+        $(pageForm).find("input[name='pageNum']").val($(this).attr("href"));
+        $(pageForm).submit();
+    });
 
+    $("a.block2-btn").click(function(e){
+        e.preventDefault();
+        location.href = "/product/detail" + paging + "&pno="+$(this).attr('href');
+    });
 
 
         if(pno != null){
